const Discord = require('discord.js');

module.exports = {
  commands: ['script'],
  group: 'Settings',
  description: 'Скрипт',
  usage: '',
  minArgs: 0,
  maxArgs: null,
  callback: async (message, args, text, commandText, bot) => {
    try {
    } catch {
      return;
    }
  }
}